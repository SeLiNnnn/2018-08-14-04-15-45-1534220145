// Write your cade below:
module.exports = function main(a, b) {
  return a % b

  // 我还以为不能直接用%来计算...
  // if (b === 0) {
  //   console.log("除数不能为0")
  // } else {
  //   return a - Math.floor(a / b) * b
  // }
}
